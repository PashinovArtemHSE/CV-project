version: "3.9"

services:
  app:
    build: .
    container_name: my_flask_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    command: >
      sh -c "python --version && python app.py"